# Sesión del 17 de mayo 2026 — Lo que se hizo

## Evaluación inicial del proyecto
- Score inicial: **420 / 1000**
- Código bien escrito pero sin infraestructura real desplegada
- Google Maps funciona en mobile (key real configurada)
- Firebase API key del mobile sigue siendo placeholder
- Base de datos solo en local (Docker)
- Backend no compilado ni desplegado

## CVE Scan (npm audit) — completado
- Vulnerabilidades iniciales: **58** (2 críticas, 26 high)
- Vulnerabilidades finales: **17** (0 críticas, 1 high en CLI de Expo — no producción)
- Commit: `71b639a`

**Cambios aplicados:**
- `firebase-admin` v10 → v13 en backend (fix CRÍTICO: ejecución código arbitrario)
- `axios` v1.6.7 → v1.16.1 en backend, mobile, dashboard (fix SSRF, auth bypass)
- `next` v14 → v16 en dashboard (fix XSS, middleware bypass)
- `firebase` client SDK v10.8 → v12.13 en mobile
- `jest-expo` v47 → v55 en mobile
- `xlsx` eliminado del dashboard (sin fix disponible, no se usaba)
- `firebase-admin` y `next` eliminados del mobile (no debían estar ahí)
- `sendMulticast` → `sendEachForMulticast` en `firebase.ts` (API v13)

## Security Deep Dive Architecture — completado
- 19 hallazgos reales en código (2 críticos, 8 altos, 5 medios, 4 bajos)
- Commit: `eba5565`

**Fixes aplicados:**
- `database.ts`: SSL `rejectUnauthorized: false` → `true` (previene MITM)
- `driver.routes.ts`: Bug corregido — ruta pública de conductores bloqueada por middleware (pasajeros no podían ver perfiles)
- `index.ts`: JWT redactado en logs de Morgan en producción
- `index.ts`: Helmet con CSP y referrerPolicy explícitos
- `index.ts`: CORS filtra localhost automáticamente en producción
- `env.ts`: JWT mínimo 64 chars (antes 32)
- `paymentController.ts`: Idempotencia en webhook Stripe via Redis (previene pagos dobles)
- `fcmService.ts`: `sendMulticast` → `sendEachForMulticast` (firebase-admin v13)

**Pendiente manual (requiere credenciales externas):**
- Rotar JWT secrets: `openssl rand -base64 64`
- Implementar tabla `admin_users` con passwords hasheados
- Actualizar Expo v54 → v55 cuando haya tiempo de probar

## UOM / KLOC Scan — completado
- Total código real: **23.9 KLOC** (23,947 líneas sin JSON ni node_modules)
- Herramienta: `npx cloc`
- Desglose: Mobile ~11.5 KLOC, Backend ~7.0, Dashboard ~3.5, SQL ~1.0, Otros ~0.9

## Score final del día
- Score actualizado: **480 / 1000** (subió 60 puntos)

## Pendiente para mañana — INFRAESTRUCTURA

Para llegar a 700+ puntos y poder operar:

1. **Supabase** — crear proyecto, habilitar PostGIS, correr las 10 migraciones SQL de `database/migrations/`
2. **Railway** — desplegar backend, agregar Redis, configurar todas las variables de entorno
3. **EAS Build** — `eas init`, `eas build --platform android`, `eas build --platform ios`
4. Actualizar `mobile/.env` con URL real de Railway
5. Subir a Google Play y App Store
